import styled from '@emotion/styled';

export const _ModalTitle = styled.h2`
  font-family: 'Montserrat', Arial, sans-serif;
  font-weight: 800;
  font-size: 3.2rem;
  line-height: 3.6rem;
  color: #000000;
  margin: 0 0 2rem;
`;

export const _ModalText = styled.p`
  font-weight: 600;
  font-size: 1.4rem;
  line-height: 1.8rem;
  color: #000000;
  margin-bottom: 2.4rem;
`;
